IDLE中交互模式查看内置函数:dir(__builtins__)
ALT+P回到上一句代码
ALT+N去到下一条语句
TAB根据已有字符推测你要选择的函数
help()函数名可以得到函数的解释
CTRL+J       #不会出现>>>的换行
CTRL+ENTER   #出现>>>的换行

True代表1，False代表0.True+True输出2.
但是非0也表示真。

将str,int等作为变量名时不会出错，但是再使用str和int时只会作为变量名，失去原有作用。

import导入模块，类似c语言include。
